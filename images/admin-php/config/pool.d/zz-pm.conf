[www]
; tuning of the process manager
pm = dynamic
pm.max_children = 25
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

; Enable the FPM status page
pm.status_path = /fpm-status

; Enable the monitoring page of FPM.
ping.path = /fpm-ping
ping.response = pong